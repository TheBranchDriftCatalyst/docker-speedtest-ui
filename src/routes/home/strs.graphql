# TODO:
#   * Add time period
#   * Add group by (non-resample)
query GetAllSTRs($input: SpeedTestResultsInput) {
  SpeedTestResults(input: $input) {
    count
    timing
    data {
      download
      upload
      timestamp
      index
    }
  }
}
